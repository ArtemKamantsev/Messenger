<Window x:Class="Messenger.SignInWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Messenger"
        mc:Ignorable="d"
        Title="Sing In"
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource CommonWindow}">
    <Grid>
        <TextBlock Name="tbClose"
                   Style="{StaticResource CloseBtn}"
                   MouseDown="TbClose_MouseDown"/>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Text="Enter your name:" FontSize="25" Margin="0 5 0 5"/>
            <TextBox Name="tbUserName" FontSize="25" Grid.Row="1" Margin="0 5 0 5" />
            <Button Name="btnNext" Content="Sign In" Grid.Row="2" FontSize="25" Margin="0 5 0 5" Click="BtnNext_Click"/>
        </Grid>
    </Grid>
</Window>
